{
    "layer": "top",    // Waybar at top layer
    "position": "top", // Waybar position (top|bottom|left|right)
    "height": 20,      // Waybar height (to be removed for auto height)
    // "spacing": 2,   // Gaps between modules (4px)

    // Choose the order of the modules
    "modules-left": ["sway/workspaces"],
    "modules-center": [],
    "modules-right": ["memory", "cpu", "temperature", "pulseaudio", "bluetooth", "network", "idle_inhibitor", "battery", "clock"],
    
    "sway/workspaces": {
	"disable-scroll": true,
	"disable-click": true,
    },
    
    "tray": {
        "spacing": 10,
    },

    "idle_inhibitor": {
        "format": "{icon}",
        "format-icons": {
            "activated": "",
            "deactivated": ""
        }
    },
    
    "clock": {
	    "format": "{:%a %d %b %R}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "cpu": {
        "format": " {usage}%",
        "tooltip": false
    },

    "memory": {
        "format": " {}%"
    },

    "temperature": {
        "critical-threshold": 75,
        "format": "{icon} {temperatureC}°C",
        "format-icons": ["", "", ""]
    },

    "battery": {
	"bat": "BAT0",
	"full_at": 95,
        "states": {
            "good": 95,
            "warning": 20,
            "critical": 10
        },
        "format": "{icon} {capacity}%",
        "format-charging": " {capacity}%",
        "format-icons": ["", "", "", "", ""]
    },

    "network": {
        "format-wifi": " ",
        "format-ethernet": "ﯱ ",
        "tooltip-format": "ssid: {essid} \nadd: {gwaddr} ({ifname}) \nsignal: {signalStrength}%",
        "format-linked": " ",
        "format-disconnected": "⚠",
    },

    "pulseaudio": {
        "format": "{icon}",
        "format-muted": "婢",
        "format-bluetooth}": "", 
        "format-bluetooth-muted": "婢",
        "format-icons": {
            "headphone": " ",
            "default": ["奄", "奔", "墳"]
        },
    },

    "bluetooth": {
    "format-on": "",
    "format-off": "",
    "format-connected":"",
    "tooltip-format": "name:  {controller_alias} ({controller_address})",
  	"tooltip-format-connected": "name:  {controller_alias} ({controller_address})\nconnected devices:\n{device_enumerate}",
  	"tooltip-format-enumerate-connected": "- {device_alias} ({device_address})"
    }

}

